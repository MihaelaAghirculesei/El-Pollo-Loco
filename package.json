{
  "name": "el-pollo-loco",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r index.html impressum.html impressum.css style.css fonts audio img_pollo_locco js models levels dist/"
  }
}
